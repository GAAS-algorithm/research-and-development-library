{
  "name": "research-and-development-library",
  "private": true,
  "scripts": {
    "build": "cd app && npm run build",
    "deploy": "npm run build && firebase deploy",
    "dev": "cd app && npm run dev"
  }
}
